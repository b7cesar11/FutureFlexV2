# Future Flex V2 — frontend environment
# Same-origin deployments need no value here: the app defaults to /api.
# For split frontend/backend deployments set the backend origin only (without /api).

REACT_APP_BACKEND_URL=
